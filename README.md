# Employee-Management-System-EMS-
“Employee Management System is a role-based system where Admin manages employee records. Authentication is implemented using JWT. Passwords are securely hashed using bcrypt. Authorization is handled through middleware based on user roles. Database operations are performed using MySQL stored procedures.”
